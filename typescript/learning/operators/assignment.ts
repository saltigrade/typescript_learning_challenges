let number = 4;
number += 5;
console.log("Výsledek je: " + number);

let a = 10;
let b = 15;

// * Chci přičíst hodnotu b k a (a = a + b)
a += b; // Výsledek: 25
console.log("a += b => " + a); // ? a = 25

a -= b; // ? 25 - 15 = 10; hodnota a je: 10
console.log("a -= b => " + a);
